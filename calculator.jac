"""Simple  Calculator"""

with entry:__main__ {
    print("🖩 Welcome to my Calculator jas app!");

    # Define two numbers
    a = 10;
    b = 5;

    # Perform operations
    sum = a + b;
    diff = a - b;
    prod = a * b;
    quot = a / b;

    # Print results
    print(f"{a} + {b} = {sum}");
    print(f"{a} - {b} = {diff}");
    print(f"{a} * {b} = {prod}");
    print(f"{a} / {b} = {quot}");
}
